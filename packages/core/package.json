{
  "name": "@alexandria/core",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo task wrapper for the Python alexandria-core package (driven by uv).",
  "scripts": {
    "build": "uv build --package alexandria-core --out-dir dist",
    "test": "uv run pytest tests"
  }
}
