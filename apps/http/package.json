{
  "name": "@alexandria/http",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo task wrapper for the Python alexandria-http app (driven by uv).",
  "scripts": {
    "build": "uv build --package alexandria-http --out-dir dist",
    "test": "uv run pytest tests",
    "dev": "uv run python -m api"
  }
}
