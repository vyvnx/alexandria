{
  "name": "@alexandria/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo task wrapper for the Python alexandria-engine app (driven by uv).",
  "scripts": {
    "build": "uv build --package alexandria-engine --out-dir dist",
    "test": "uv run pytest tests",
    "dev": "uv run python -m engine"
  }
}
