{
  "name": "@alexandria/api",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo task wrapper for the Python alexandria-api app (driven by uv).",
  "scripts": {
    "build": "uv build --package alexandria-api --out-dir dist",
    "test": "uv run pytest tests",
    "dev": "uv run python -m src"
  }
}
